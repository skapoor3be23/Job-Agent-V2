"""AI Job Opportunity Intelligence Agent — v2 architecture.

Lives alongside the original pipeline (src/live_pipeline.py), which remains
the fallback until this package is validated. See src/agent/graph.py.
"""
__all__ = ["config", "graph", "schemas"]
